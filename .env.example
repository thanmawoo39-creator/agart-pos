# =============================================================================
# AgartPOS Environment Configuration
# =============================================================================
# Copy this file to .env.local for development or .env.production for builds
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration (REQUIRED for Android/Capacitor builds)
# -----------------------------------------------------------------------------
# The base URL for API requests. This is CRITICAL for mobile builds.
#
# For LOCAL development (desktop browser):
#   Leave empty - Vite proxy handles /api/* requests
#
# For LOCAL Android testing (emulator):
#   VITE_API_URL=http://10.0.2.2:5000
#
# For LOCAL Android testing (physical device on same network):
#   VITE_API_URL=http://192.168.1.100:5000  (use your PC's LAN IP)
#
# For PRODUCTION (VPS/Cloud server):
#   VITE_API_URL=https://api.yourdomain.com
#
# For SERVEO tunnel:
#   VITE_API_URL=https://your-subdomain.serveo.net
#
VITE_API_URL=

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# SQLite database file path (relative to project root)
DATABASE_URL=./database.sqlite

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port for the Express server
PORT=5000

# Node environment
NODE_ENV=development

# -----------------------------------------------------------------------------
# AI/ML Features (Optional)
# -----------------------------------------------------------------------------
# Google Gemini API key for AI image recognition
GEMINI_API_KEY=

# Groq API key for fast AI inference
GROQ_API_KEY=

# Local AI server URL (e.g., Ollama)
LOCAL_AI_URL=http://localhost:11434

# -----------------------------------------------------------------------------
# Firebase (Optional - for push notifications)
# -----------------------------------------------------------------------------
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=
